{
  "name": "spotisaasv2",
  "version": "1.0.0",
  "description": "Spotify Likes Sync - Automatically sync liked songs to a playlist",
  "scripts": {
    "sync": "node --env-file=.env.local -r ts-node/register src/sync.ts",
    "test": "vitest run",
    "test:integration": "node --env-file=.env.local -r ts-node/register src/verify-playlist.ts"
  },
  "keywords": [
    "spotify",
    "sync",
    "playlist"
  ],
  "author": "Alexis",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
